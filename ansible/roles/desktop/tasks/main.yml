---
- import_tasks: user.yml
  tags: user

- import_tasks: fonts.yml
  tags: fonts

- import_tasks: apps.yml
  tags: apps

- import_tasks: file_picker.yml
  tags: file_picker

- import_tasks: gnome.yml
  tags: gnome

# TODO: backup / restore dconf settings
# wallpaper
# font scaling - inc bookwright
