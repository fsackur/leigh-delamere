[Unit]
Description=x11vnc server
After=multi-user.target

[Service]
Type=simple
ExecStart=/usr/bin/x11vnc -auth guess -forever -loop -noxdamage -repeat -rfbauth {{ vnc_hashed_passwd_path }} -rfbport 5900 -shared

[Install]
WantedBy=multi-user.target
